================================================================================
 revswarm — 分散式爬蟲：收集台股「每月營收公布日期」
 handoff / todo  (建立於 2026-07-11，承接前一段探索對話)
================================================================================

這份文件是自足的交接文件。新對話從這裡開始即可，不需要前面的脈絡。
包含：專案目標、資料源探索的結論(重要,別重走冤枉路)、可直接用的爬取配方、
架構設計(server+worker)、schema/API、必做的補強、建置步驟、現有可重用資產。


================================================================================
1. 專案目標
================================================================================
為「營收公布日 vs 股價反應」事件研究，收集台股每月營收的【精確公布日期】。
- 範圍：約 1848 檔股票 (上市+上櫃+興櫃)，民國 109/1 ~ 115/1 (=73 個月)
        → 約 134,904 個 (股票 x 月份) 任務
- 需求：日期精確到「日」(有時間更好，但 day-level 已足夠事件研究)
- 股票清單現有：/home/nvidia/poyi/playground/active_stocks.txt  (1848 個代號，一行一個)


================================================================================
2. 資料源探索結論 (⚠️ 重要：這些是踩過雷得到的，別重走)
================================================================================

2.1 為什麼不用 MOPS：
  - MOPS「歷史重大訊息」t05st01 只涵蓋「自願把月營收發成重大訊息」的公司，
    抽樣全 1848 檔用單一年度掃描，只有約 32 家有資料 (台積電/聯發科/台塑/中鋼/
    台塑化…)。這 32 家有精確到「時、分」的時間戳，可當免費精確樣本，但涵蓋太少。
  - MOPS 的結構化營收報表 (t21sc04_ifrs 彙總表 / t05st10_ifrs 逐檔) 與 Yahoo/
    MoneyDJ 的結構化營收頁：都【只有年月+金額，沒有公布日期】。
  → 結論：公布日期只存在於「新聞文章」裡。

2.2 選定資料源 = Yahoo 台灣「搜尋」(tw.search.yahoo.com/search)
  - 搜尋「{公司名} {年}年{月}月」會把該月營收的新聞文章帶出來，文章日期就是公布日。
  - 日期準確度已驗證：MoneyDJ/Yahoo 文章日期 == MOPS 申報日期(同一天)。
    例：台積電 2022年1月營收，MOPS 申報 2022-02-10 13:41，MoneyDJ 文章 2022-02-10 13:56。
  - 涵蓋所有公司(連 MOPS 重大訊息查不到的穩懋等都有)。

2.3 查詢字串的兩個關鍵教訓：
  (a) 【不要】在查詢後面加「營收」兩個字！
      加了會讓 Yahoo 去比對「最近的營收新聞」→ 回傳近期(錯年份)文章，recall 慘掉。
      正確：  "台積電 109年8月"          (對)
      錯誤：  "台積電 109年8月營收"       (recall 掉一半)
  (b) 【民國年】和【西元年】兩種都要查，取聯集 — 它們釣到不同來源、互補：
      - 民國年 "穩懋 109年1月"  → 釣到 MoneyDJ 文章 (標題用民國年:「穩懋 109年1月營收…」)
      - 西元年 "台塑 2020年6月"  → 釣到 Yahoo股市【公告】(標題用西元年:「【公告】台塑 2020年6月合併營收…」)
      實測：先民國年、漏抓的再西元年，救回一大半漏抓。合併涵蓋率估 ~90%+。

2.4 「窗過濾」— 消滅髒資料的關鍵 (實測可把髒資料清成 0)：
  月營收依規定次月10日前申報，遇假日順延。所以正確公布日一定落在：
      營收月的「次月」的 1~15 號。
    - roc_month <= 11 : 公布日應在 西元(roc_year+1911) 年、(roc_month+1) 月、1~15 日
    - roc_month == 12 : 公布日應在 西元(roc_year+1912) 年、1 月、1~15 日
    (11、13 號常見=週末順延；15 號=1月營收遇春節順延。>15 號的一律丟棄)
  任何抓到但不在此窗的日期 = 丟棄(當作沒抓到)。這保證 0 髒資料。
  ⚠️ server 端也要用這個窗再驗證一次 worker 回傳的日期，不要全信 worker。

2.5 解析陷阱 — 公司名是子字串會撞：
  「統一」 vs 「統一超」、「台塑」 vs 「台塑化」。必須【精確名稱比對】：
  鎖定標題形如「{名稱} {年}年{月}月」或「【公告】{名稱} {年}年{月}月」，
  且名稱後面緊接空白/年，不可被更長的名稱吃掉。

2.6 rate-limit / 封鎖 (這是唯一真正的操作瓶頸，也是為何要做分散式)：
  - 純腳本 curl 對 Yahoo 必須加 --http1.1！(HTTP/2 在某些環境會 SSL unexpected eof、
    連不上，回 HTTP 000。加 --http1.1 就正常 200。)
  - 單一 IP 持續高頻查詢 → 連線被 reset (SSL EOF / 回傳 size=0 / 非200)。實測狂打一天
    後單 IP 幾乎全被擋。→ 解法就是把 worker 分散到多台機器/多個 IP (= revswarm 的核心)。
  - rate-limited 的訊號：curl 失敗 / 非200 / 頁面 < ~2000 bytes。
    ⚠️ 這種【不可】回報 failed，要退避重試 (見架構)。

2.7 pilot 實測數據 (10檔 x 民國109年，供參考):
  - 修正查詢(不加營收)+窗過濾：連得上的請求 ~78% 正確、0 髒。
  - 西元年補查再救回漏抓的約一半。
  - 每次查詢純抓取 ~0.8~1.5s (不含退避)。單IP會被封是主要限制。


================================================================================
3. 爬取配方 (worker 對「單一 (股票,月份) 任務」該做的事) — 可直接照做
================================================================================
輸入：stock_id, name, roc_year, roc_month   (roc_* 是「營收所屬」月份)

步驟：
(1) 算期望窗 expected_window(roc_year, roc_month):
      if roc_month <= 11: wy = roc_year+1911 ; wm = roc_month+1
      else              : wy = roc_year+1912 ; wm = 1
      合法日 = wy 年 wm 月 1~15 日

(2) 依序嘗試查詢字串 (中一個就停)：
      q1 = f"{name} {roc_year}年{roc_month}月"          # 民國年 → MoneyDJ
      q2 = f"{name} {roc_year+1911}年{roc_month}月"      # 西元年 → Yahoo【公告】

(3) 抓取 (務必 --http1.1)：
      curl -sS --http1.1 -m 25 -G "https://tw.search.yahoo.com/search" \
        --data-urlencode "p={query}" \
        -A "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0 Safari/537.36" \
        -H "Accept-Language: zh-TW,zh;q=0.9"

(4) 分類回應：
      - curl 非0 退出 / http!=200 / len(html) < 2000  → RATE_LIMITED (退避、稍後重試，不算 failed)
      - 否則 → 進行解析

(5) 解析 (窗過濾 + 精確名稱)：
      - 找頁面所有日期： 20\d{2}年\d{1,2}月\d{1,2}日  (也處理 20\d{2}/\d{1,2}/\d{1,2}、20\d{2}-\d{2}-\d{2})
      - 只留落在 expected_window (wy年wm月、日1~15) 的日期
      - 優先取「最接近一個精確標題錨點」的窗內日期；錨點 regex 例：
          f"{re.escape(name)}[ ]?{roc_year}年\\s*{roc_month}月營收"      (民國/MoneyDJ)
          f"【公告】{re.escape(name)}[ ]?{roc_year+1911}年\\s*{roc_month}月"  (西元/Yahoo)
        名稱後須接空白或數字，避免「統一」吃到「統一超」。
      - 有窗內日期 → SUCCESS(date, source, raw_title)
      - 兩個查詢都試完、頁面正常但無窗內日期 → FAILED (真的找不到)

(6) 禮貌與退避：每次查詢間 delay + 隨機抖動 (建議 3~5s)。偵測到 RATE_LIMITED → 指數退避
    (甚至該 worker 暫停一陣子，因為多半是整個 IP 被擋)。

已驗證可用的解析核心 (Python 片段，供 worker 參考)：
------------------------------------------------------------
import re, subprocess, time
UA="Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0 Safari/537.36"
def expected_window(ry, rm):
    return (ry+1911, rm+1) if rm<=11 else (ry+1912, 1)
def fetch(q):
    r=subprocess.run(["curl","-sS","--http1.1","-m","25","-G",
        "https://tw.search.yahoo.com/search","--data-urlencode",f"p={q}",
        "-A",UA,"-H","Accept-Language: zh-TW,zh;q=0.9"],
        capture_output=True,text=True,timeout=30)
    h=r.stdout
    if not h or len(h)<2000: return None       # RATE_LIMITED 訊號
    return h
def parse(h, name, ry, rm):
    wy,wm=expected_window(ry,rm)
    anchors=[m.start() for m in re.finditer(rf'(?:【公告】)?{re.escape(name)}[ ]?\d+年\s*{rm}月', h)]
    cand=[]
    for dm in re.finditer(r'(20\d{2})年(\d{1,2})月(\d{1,2})日', h):
        y,mm,dd=int(dm.group(1)),int(dm.group(2)),int(dm.group(3))
        if y==wy and mm==wm and 1<=dd<=15:
            cand.append((dm.start(), f"{y:04d}-{mm:02d}-{dd:02d}"))
    if not cand: return None
    if anchors:
        return min(cand,key=lambda c:min(abs(c[0]-a) for a in anchors))[1]
    return cand[0][1]
------------------------------------------------------------


================================================================================
4. 架構：分散式工作佇列 (server 派工 + 多機 worker)
================================================================================
概念：server 持有全部 (股票,月份) 任務；worker 可在任何機器啟動，向 server 租一批任務，
爬到就回報 success/failed，被限流就回報 rate_limited(放回佇列，不算失敗)。多機=多IP，
繞過單IP被封。

State machine:  undone → dispatched → (success | failed)
  - dispatched 超過 10 分鐘沒回覆 → 自動變回 undone (租約逾時)
  - failed = 「真的找不到」(兩種年份都試過)，不再重試
  - rate_limited ≠ failed：立刻放回 undone，worker 退避

4.1 SQLite schema (建議)：
------------------------------------------------------------
CREATE TABLE tasks(
  id            INTEGER PRIMARY KEY,
  stock_id      TEXT NOT NULL,
  name          TEXT NOT NULL,
  roc_year      INTEGER NOT NULL,   -- 營收所屬(民國)年
  roc_month     INTEGER NOT NULL,   -- 營收所屬月
  state         TEXT NOT NULL DEFAULT 'undone',   -- undone|dispatched|success|failed
  announce_date TEXT,               -- 'YYYY-MM-DD' (success 時)
  source        TEXT,               -- 'moneydj' | 'yahoo' | ...
  raw_title     TEXT,               -- provenance，抓到的標題原文
  attempts      INTEGER DEFAULT 0,
  fail_count    INTEGER DEFAULT 0,
  dispatched_at INTEGER,            -- epoch 秒，用於租約逾時
  worker_id     TEXT,
  updated_at    INTEGER,
  UNIQUE(stock_id, roc_year, roc_month)
);
CREATE INDEX idx_state ON tasks(state);
-- 建議開 WAL：PRAGMA journal_mode=WAL;
------------------------------------------------------------

4.2 API endpoints (FastAPI 建議)：
  POST /lease?n=30&worker=<id>
     - 惰性回收+原子鎖定：一次交易內挑 n 筆 state='undone'
       OR (state='dispatched' AND dispatched_at < now-600)，標記 dispatched、記 worker/時間，回傳批次
     - SQLite 原子作法：BEGIN IMMEDIATE; 或 UPDATE ... WHERE id IN (SELECT ... LIMIT n) RETURNING *;
     - 回傳每筆含 {id, stock_id, name, roc_year, roc_month}
  POST /result   body: {id, worker, status, date?, source?, title?}
     - status=success：server 用 2.4 的窗再驗一次 date；通過才存 success(idempotent，
       已 success 就忽略重複/逾時的遲到回報，不可把 success 降級)
     - status=failed ：attempts+1、fail_count+1；設 state=failed
                       (worker 只在「兩種年份都試過仍無」時才送 failed)
     - status=rate_limited：立刻放回 undone (release 租約)，不計失敗
  GET  /stats
     - 各 state 計數、進度%、ETA、近期成功率

4.3 worker 迴圈：
  loop:
    batch = POST /lease?n=30
    if empty: sleep, continue
    for task in batch:
        依「第3節配方」爬取
        分類 → 收集 success / failed / rate_limited
        若連續多筆 rate_limited → 判定本機 IP 被擋，指數退避 (甚至長睡)
        每筆間 delay+jitter
    POST /result (可批次回報)


================================================================================
5. 必做的補強 (前一輪 review 的重點，別漏)
================================================================================
🔴 關鍵：
  1. failed 不可太早判：worker 必須「民國年+西元年」兩種都試過都沒中才回 failed。
     (實測很多漏抓改西元年就找到) 保留把所有 failed 重開做「最後一輪」的管理指令。
  2. worker 要可靠區分 rate_limited(連線失敗/非200/頁<2000B) vs 真failed(正常頁但無窗內日期)。
  3. 派工必須原子操作 (BEGIN IMMEDIATE / UPDATE...RETURNING)，避免兩隻 worker 拿到同一筆。
  4. 租約回收用「惰性回收」(派工 SQL 順便撿逾時的)，免背景執行緒。
  5. server 端用窗(2.4)再驗證 date 才存 success，防 worker bug 污染。
🟡 實務：
  6. 任務要帶「公司名稱」；解析要精確名稱比對(統一≠統一超)。
  7. 派工用批次(一次20~50筆)減少 server 往返(13.5萬筆別一筆一次)。
  8. 記錄 source 與 raw_title (provenance，研究資料要可追溯)。
  9. worker 自我限速(delay+jitter)+偵測封鎖後退避；別讓單 worker 燒掉自己 IP。
  10. server 要能被任意機器連到 + 簡單 token 驗證 (避免亂入)。部署: VPS / tailscale / ngrok。
  11. ⚠️ 大量爬 Yahoo 可能違反其 ToS，也會招致更強封鎖 — 務必節制、分散、加延遲。


================================================================================
6. 建置步驟 (下一段對話要做的)
================================================================================
[ ] 1. 建 code→name 對照表 stocks.csv (1848 檔)。來源: 證交所 ISIN
       (isin.twse.com.tw) 或 TWSE/TPEX OpenAPI。欄位: stock_id, name, market(sii/otc/rotc)。
       (worker 查詢需要中文簡稱)
[ ] 2. server: FastAPI + SQLite，實作 /lease /result /stats + 第4節 schema/邏輯 + 第5節補強。
[ ] 3. init 腳本: 讀 active_stocks.txt + stocks.csv，展開 1848 x (109/1~115/1)=~135k 筆 tasks(undone)。
[ ] 4. worker: 依第3節配方 (curl --http1.1、民國+西元雙查、窗過濾、精確名稱、退避)，
       可帶 --server URL 在任意機器跑。
[ ] 5. 小規模端到端測試 (先幾檔)，確認 server/worker/租約/回收/驗窗都對，再放量。
[ ] 6. (可選) 把那 32 家 MOPS 精確資料(含時分)併入，當高信度基準/交叉驗證。
[ ] 7. (可選) 監看儀表板 (讀 /stats)。

技術備忘：
  - curl 打 Yahoo 一定要 --http1.1 (否則 HTTP/2 SSL EOF)。
  - Yahoo 搜尋 URL: https://tw.search.yahoo.com/search?p=<查詢>  (用 --data-urlencode 處理中文)
  - Python 標準庫足夠 (urllib 對 Yahoo 可能也有 h2 問題，保險起見 worker 直接呼叫 curl --http1.1)。


================================================================================
7. 現有可重用資產 (在 /home/nvidia/poyi/playground/)
================================================================================
  - active_stocks.txt          : 1848 檔股票代號 (一行一個)。→ 複製進 revswarm。
  - mops_revenue_dates.py       : MOPS t05st01 爬蟲 (那32家自願揭露公司的精確日期+時間+盤後旗標)。
                                  可當「高信度基準/交叉驗證」用，或當 fallback。內含 MOPS API 細節:
                                  POST https://mops.twse.com.tw/mops/api/t05st01
                                  body={"companyId","year"(民國),"month":"all","firstDay":"","lastDay":""}
                                  (firstDay/lastDay 兩個 key 即使空字串也必須存在)
  - 2330.csv                    : 台積電 109~115 月營收公布日範例輸出(含精確時分)。

MOPS 那條路的「純月營收」判準(若之後要用 t05st01 資料)：
  主旨含營收/營業額/營業收入+指定年月，且不含盈餘/損益/更正/差異，且公布日<=15。
  (中鋼「自結合併營收及稅前盈餘」那種要排除；台塑「合併營業額」在10號前要保留)


================================================================================
8. 一句話總結給新對話
================================================================================
做一個 server(FastAPI+SQLite 工作佇列，undone/dispatched/success/failed，10分租約、惰性回收、
原子派工、窗驗證)，加可跨機跑的 worker(curl --http1.1 搜 Yahoo、民國+西元雙查、次月1~15號窗過濾、
精確名稱比對、rate_limited退避不算failed)，把 1848 檔 x 109/1~115/1 的月營收公布日爬回來。
資料源與所有雷點見上。先做 stocks.csv + server + init + worker，小規模驗證後放量。

（以上 1~8 節為 2026-07-11 的初始交接文件，Yahoo 主線已依此完成。以下第 9 節是
  2026-07-29 新增的下一輪任務：用 Google 補搜 Yahoo 救不回的 failed。）


================================================================================
9. 下一輪：用 Google 補搜 failed (Playwright) — 2026-07-29 規劃，新 session 執行
================================================================================

9.1 現況數據 (Yahoo 主線已全部跑完)
--------------------------------------------------------------------------------
  總任務        134,904
  prelisting      6,194   (上市前，不可能有月營收，排除在分母外，見 mark_prelisting.py)
  應做 workable 128,710
  success        97,833   = 76.01%   ← 目前勝率
  failed         30,877   ← 本輪要補搜的目標
  進度 100% (done = success + failed = workable)

  目標：用 Google 補搜這 30,877 筆。實測命中率 9/10 = 90%。
  推估：救回 ~27,800 → 總成功率 ~97.6% (樂觀) / 70% → 92.8% (保守)


9.2 資料源探索結論 (⚠️ 全部實測過，別重走這些冤枉路)
--------------------------------------------------------------------------------
✗ Google Custom Search JSON API
    已對新客戶關閉，2027-01-01 全面停用。一律回：
      403 PERMISSION_DENIED "This project does not have the access to Custom Search JSON API."
    試過 3 把金鑰、跨不同專案、確認 API 已啟用、金鑰無限制/有限制 — 全無效。
    注意這個錯誤訊息跟標準的「API 未啟用」不同(後者會寫出專案編號)，別誤判成設定問題。
    → 不要再申請金鑰、不要再開新專案，這條路是死的。

✗ curl 直接爬 www.google.com/search
    每次都被擋(不是頻率問題)，回「請啟用 JavaScript」轉址頁 /httpservice/retry/enablejs。
    原因：回應帶一串 Accept-CH: Sec-CH-UA-* ，Google 在核對 Client Hints/TLS 指紋，
    curl 帶 Chrome UA 也偽裝不了。換 proxy IP 完全無效。
    (加 CONSENT cookie、gbv=1 都試過，無效。)

✗ Bing 直接爬 www.bing.com/search
    立即跳驗證碼「請解決以下挑戰以繼續」。

✗ DuckDuckGo html.duckduckgo.com/html/
    第 1 次請求成功(可拿到 10 筆結果)，第 2 次起就 403 封 IP。
    實測 60 次請求 59 次被擋；之後單發也是 403(236 bytes 錯誤頁)。單 IP 不可行。

✗ MoneyDJ 直取
    /z/zc/... 資料頁 302 轉 SSO 登入頁；首頁是 Big5 編碼且 curl 拿不到任何可爬連結
    或站內搜尋端點；公開新聞頁網址是不可預測的 GUID (NewsViewer.aspx?a=<guid>)。
    → 仍然需要外部索引才找得到，繞回原點。

✗ MOPS 官方 (詳見 2.1)
    重大訊息 t05st01 只涵蓋約 32 家自願把月營收發成重大訊息者，mops_baseline.csv 約 53 檔。
    無法覆蓋 3 萬筆。

✓ 可行解：Playwright 驅動「系統安裝的 Chrome」+ headful + 持久設定檔
    真實 Chrome 通過了 Client Hints 指紋檢查(curl 過不了的那關)。
    headless=True 曾被導到 /sorry/ 驗證頁；改 headful + 持久設定檔後 0 驗證碼。


9.3 可行配方 (實測驗證過，直接照做)
--------------------------------------------------------------------------------
環境：
  pip3 install --user playwright         # 1.61.0；這是本 repo 第一個第三方依賴
  不需要 playwright install chromium     # 用系統的 /usr/bin/google-chrome (實測 Chrome 146)
  需要圖形環境：DISPLAY=:0 (headful)

啟動瀏覽器 (跨任務重用同一個實例，啟動成本高，不要每筆重開)：
  ctx = p.chromium.launch_persistent_context(
      os.path.expanduser("~/.cache/revswarm-chrome"),   # 持久設定檔，保留 cookie
      channel="chrome",            # 用系統 Chrome，非下載的 Chromium
      headless=False,              # ⚠️ 有視窗；headless 會被導到 /sorry/
      locale="zh-TW", timezone_id="Asia/Taipei",
      viewport={"width": 1366, "height": 900},
      args=["--disable-blink-features=AutomationControlled"])

查詢：
  https://www.google.com/search?q=<查詢>&hl=zh-TW&gl=tw
  不帶 num → 取第 1 頁預設約 10 筆結果。

⚠️ 查詢順序與 Yahoo 相反：Google 要「西元年優先」
  q_ad  = "{代號} {名稱} {西元年}年{月}月"    ← 第一順位 (單獨命中率 70%)
  q_roc = "{代號} {名稱} {民國年}年{月}月"    ← 第二順位 (補 20%)
  中一個就停。絕不加「營收」二字(同 2.3a)，也絕不加 "moneydj"(見 9.4)。
  Google 會直接忽略民國年 token(頁面會顯示「缺少字詞：110」)，這就是要用西元年的原因。

取文字與解析 (與 Yahoo 完全同一套，不需要新 parser)：
  text = page.inner_text("body")          # 整頁可見文字，含標題/摘要/麵包屑/相關搜尋
  hit  = revlib.parse(text, name, ry, rm) # 窗過濾(次月1~15) + 精確名稱錨點

禮貌延遲：每次查詢間 6~10 秒 (使用者指定)。

驗證碼判定：
  page.url 含 "/sorry/" → 判 rate_limited，⚠️ 絕不可判 failed
  (failed 只能是「頁面正常、兩種年份都試過、仍無窗內日期」)
  headful 之下可人工解，解完自動接續。


9.4 實測數據 (10 筆真實 failed 樣本；累計 40 次查詢，0 驗證碼)
--------------------------------------------------------------------------------
  q_ad   命中 7/10 = 70%
  q_roc  命中 5/10 = 50%
  任一    命中 9/10 = 90%   ← 外推依據

  兩種寫法互補的真正原因(不是隨機波動)：
    中央社【公告】標題寫西元年(「友達2022年3月合併營收280.43億元」) → 只有 q_ad 抓得到
    MoneyDJ 標題寫民國年  (「凌通110年10月營收3.46億、年增14.66%」) → 只有 q_roc 抓得到

  命中來源分佈：中央社【公告】4、MoneyDJ 2、CMoney 1、winvest.tw 1、Yahoo Finance 1
    → 來源很分散，MoneyDJ 只佔少數。

  ⚠️ 實測「查詢字串尾端加 moneydj」會變差，已驗證不要加：
     q_ad 70%→40%、q_roc 50%→70%、任一 90%→80%
     原因：過度限縮，把 CMoney/中央社/Yahoo 來源的命中排擠掉了(友達整筆落空)。
     四種變體取聯集仍是 9/10，加 moneydj 沒補到任何新案例，純淨損失。

  唯一始終落空：6742 澤米 民國109/5 (最舊的月份之一)


9.5 已完成的 server 端改動 (已測試，直接沿用，不用重做)
--------------------------------------------------------------------------------
  - tasks 表新增 engine 欄位 TEXT NOT NULL DEFAULT 'yahoo'
    (CREATE TABLE 與 _migrate() 兩處都加了，既有 DB 會自動 ALTER 補上)
  - 新增索引 idx_undone_engine_age(state, engine, roc_year, roc_month)
    ⚠️ 這個索引必須建在 _migrate() 內、且在 ALTER TABLE 之後。放在 SCHEMA 字串裡會讓
       舊 DB 開啟時炸「no such column: engine」，因為 executescript 跑在 _migrate 之前。
  - Store.lease(n, worker_id, engine="yahoo")  依 engine 過濾派工
  - Store.requeue_failed(engine=None)
      engine=None      → 沿用舊行為，只改 state，不動 engine
      engine='google'  → 連 engine 一併轉過去，交給 google worker 專門處理
  - POST /lease?n=&worker=&engine=google
    POST /admin/requeue-failed?engine=google
  - 惰性回收逾時租約時「不分 engine」(租約逾時就該放回，不管誰拿走的)
  - test_server.py 已補 TestEngineRouting 4 個測試；全檔 18 個測試通過
  - 實測：對 revswarm.db 副本跑 requeue_failed(engine='google') → 30,813 筆轉成
    state=undone/engine=google，yahoo 佇列不受影響

  ⚠️ 這些改動在寫下本節時「尚未 commit」，只存在於工作目錄(git status 顯示
     M server.py / M test_server.py)。新 session 開始時請先 `git status` 與
     `git diff server.py` 確認實際狀態 —— 若已被 commit 就照常繼續；若工作目錄
     被清掉了(改動消失)，則 9.5 這幾項需要重做。判斷方式：
       python3 -c "import server; print('engine' in {r[1] for r in
         server.connect('revswarm.db').execute('PRAGMA table_info(tasks)')})"
     印 True 代表 engine 欄位已在 DB 內、改動有效。


9.6 待做 (新 session 的工作)
--------------------------------------------------------------------------------
【2026-07-29 進度】Step 0~3 已完成(未 commit)：google_worker.py 改寫成 Playwright 版、
  test_google_worker.py 14 個測試、.env.example 拿掉金鑰、README 段落重寫。
  全 repo 70 個測試通過。實測 8 筆隨機 failed(多為最舊月份 109~110 年) → 命中 4 筆
  (g_ad 1、g_roc 3)，22 次查詢 0 驗證碼、0 次誤判 rate_limited。
  ⚠️ 這 50% 明顯低於 9.4 的 9/10，兩次樣本都只有 8~10 筆(CI 很寬)，且本次抽樣偏舊月份
  ——舊月份本來就最難(見 9.4 澤米)。真實命中率等放量後看 /stats。
  剩下 Step 4(啟動放量) 未做。source 標成 g_ad/g_roc(非 q_ad/q_roc)以便區分引擎。

Step 0  清理死程式碼 (目前工作目錄裡有 Custom Search API 版的殘留)
  - google_worker.py        API 版 → 改寫成 Playwright 版
  - test_google_worker.py   測 API 版 → 對應改寫
  - .env.example            刪掉 GOOGLE_API_KEY / GOOGLE_CSE_ID (Playwright 版不需金鑰)
  - README.md               google_worker 段落現在描述已死的 API → 改寫
  - .env                    裡有 3 把已無用的 Google 金鑰，可自行到 Console 刪除

Step 1  實作 google_worker.py (Playwright 版)
  - 沿用 worker.py 骨架：Client(lease/report)、主迴圈、指數退避、
    連續 rate_limited 判定被擋、提早結束本批並把剩餘任務以 rate_limited 放回
  - lease 要帶 engine=google
  - 瀏覽器實例跨任務重用；每批結束不必關閉
  - 分散式(使用者要求可在別台電腦啟動)：目標機只需
      copy google_worker.py + revlib.py
      pip3 install --user playwright
      有圖形環境 (DISPLAY)
      .env 放同一組 REVSWARM_TOKEN
      python3 google_worker.py --server http://<SERVER>:8000
  - 參數比照 worker.py：--batch/--delay/--jitter/--per-query-sleep/--rl-threshold/
    --block-sleep/--idle-sleep/--once/--max-batches
    預設 delay 改成 6，jitter 4 (=6~10 秒，使用者指定)

Step 2  test_google_worker.py
  - 比照 test_worker.py 手法：替換模組全域的 time/random/搜尋函式為假物件，
    不連網、不真的開瀏覽器、不真的睡
  - 測 crawl_task 四種分類(q_ad命中/q_roc補中/兩者皆正常但無日期=failed/被擋=rate_limited)
  - 測主迴圈退避序列與剩餘任務放回

Step 3  文件
  - README 新增 google_worker 段落：Playwright 安裝、DISPLAY 需求、驗證碼人工解流程、
    engine 佇列分流用法
  - ⚠️ 明確說明這是 repo 第一個第三方依賴，且 server.py/worker.py/revlib.py 仍維持零依賴

Step 4  啟動
  - curl -X POST -H "Authorization: Bearer $REVSWARM_TOKEN" \
      "http://<SERVER>:8000/admin/requeue-failed?engine=google"
  - 先 --once 或 --max-batches 1 小量驗證，再放量
  - 背景長跑用 tmux (不要 nohup)；headful 需要 DISPLAY=:0
  - 時程概估：每筆平均約 1.3 次查詢 × (頁面載入 2~4s + 延遲 6~10s) ≈ 12~20s/筆
    → 30,877 筆單機約 5~7 天；多機可分攤(每台一顆 IP)


9.7 未決事項 / 已知風險
--------------------------------------------------------------------------------
  1. 驗證碼：40 次查詢 0 次不代表 6 萬次安全。無人看顧時會卡住 →
     一定要有 rate_limited 退避 + 長睡，並考慮多機分攤 IP。
  2. 日期來源不一致：晶心科 111/12 的 q_ad 抓到 2023-01-09(中央社)、
     q_roc 抓到 2023-01-10(MoneyDJ)，兩者都在窗內但只有一個是真正申報日
     (可能是「新聞發稿日」vs「實際申報日」的落差)。
     若要求精確，需訂來源優先序，而不是「先命中的贏」。目前配方是先命中就停。
  3. 尚未實測：加 num=20 或翻第 2 頁(start=10) 能否救回舊月份(如澤米)。
     加 num 幾乎零成本但 Google 對它的支援不穩，需先驗證真的有變多；
     翻頁確定有效但請求數翻倍，直接反映在總時程上。
  4. 90% 來自 10 筆樣本，95% 信賴區間約 55%~100%，區間很寬。
     正式跑起來後 /stats 就是真實量測，不必另外抽樣。


9.8 一句話總結給新 session
--------------------------------------------------------------------------------
把 google_worker.py 改寫成「Playwright + 系統 Chrome + headful + 持久設定檔」爬 Google，
向 server 租 engine=google 的任務(先 requeue-failed?engine=google 把 30,877 筆轉過去)，
查詢用「西元年優先、民國年補第二」、不加 moneydj、每次間隔 6~10 秒，
沿用 revlib.parse 的窗過濾與名稱錨點，/sorry/ 判 rate_limited 絕不判 failed。
server 端的 engine 分流已完成並測試過。所有死路與實測數據見 9.2~9.4。


10. 檔案改名：worker.py → yahoo_worker.py（2026-07-30）
================================================================================
主力 worker 原本叫 worker.py，但 repo 已經有 google_worker.py（補搜 failed）與
goodinfo_worker.py（抓首次公開日），「worker」這個泛稱反而是三者裡最不清楚的一個。
改成 yahoo_worker.py，跟它吃的佇列（engine='yahoo'）同名：

  worker.py       → yahoo_worker.py
  test_worker.py  → test_yahoo_worker.py

⚠️ 只改檔名/模組名，行為與協定完全沒動：
  - CLI 參數、退避邏輯、窗過濾、lease/report 協定都一樣（lease 仍不帶 engine 參數，
    由 server 預設成 yahoo）。
  - 環境變數維持 WORKER_PROXY（不是 YAHOO_WORKER_PROXY）——已經寫在既有的 tmux/SSH
    SOCKS 流程裡，改名只會讓現有出口設定無聲失效。
  - DB 的 worker_id 仍是 hostname-pid，既有資料不受影響。

本節之前（9.x 及更早）的敘述寫的是 worker.py，那是當時的實際檔名，保留不改；
讀到時對照本節即可。
